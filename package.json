{
  "name": "review-generator",
  "version": "1.1.0",
  "description": "AI-powered software review generator for Capterra, G2, SoftwareReviews",
  "main": "server/index.js",
  "scripts": {
    "install:all": "npm install && cd client && npm install",
    "dev:server": "node server/index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1"
  }
}
